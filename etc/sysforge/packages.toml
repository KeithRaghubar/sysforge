# =============================================================================
# packages.toml — SysForge package manifest
#
# Lists packages SysForge should build and install via the packages stage.
# Manage with: sysforge packages add/remove/list/sync
#
# Fields per entry:
#   name           required  package name
#   source         required  "repo" (pacman -S) | "aur" (AUR build)
#   pkgbuild_patch optional  true = extract and patch PKGBUILD flags before build
#   cache          optional  false = disable ccache/sccache for this package
#
# AUR packages are resolved via the PKGBUILD directory set in flag_profiles.toml
# ([paths] pkgbuild_dir). Missing AUR packages are auto-cloned from the AUR.
# Repo packages are installed via pacman -S --needed (no build step) unless
# repo_mode = "profiled" is set below.
# =============================================================================

# [build]
# pkgbuild_dir = "~/src"   # override the path from flag_profiles.toml for this manifest
# repo_mode    = "pacman"  # "pacman" (default) | "profiled" — how repo-source packages are built
#                          # "pacman"   → install via pacman -S --needed (no build step)
#                          # "profiled" → build from PKGBUILD with sysforge flag profiles
#                          # per-package pkgbuild_patch = true overrides to "profiled" regardless


# =============================================================================
# SysForge recommended dependencies
#
# ccache/sccache are used automatically when configured in flag_profiles.toml
# [cache] section (auto-detect by default). reflector is used by the configure
# stage to set up /etc/pacman.d/mirrorlist.
# =============================================================================

# [[package]]
# name = "sysforge-git"
# source = "aur"

[[package]]
name = "ccache"
source = "repo"

[[package]]
name = "sccache"
source = "repo"

[[package]]
name = "reflector"
source = "repo"


# =============================================================================
# Shell and editor
# =============================================================================

[[package]]
name = "zsh"
source = "repo"

[[package]]
name = "neovim"
source = "repo"

[[package]]
name = "man-db"
source = "repo"

[[package]]
name = "man-pages"
source = "repo"


# =============================================================================
# System utilities
# =============================================================================

[[package]]
name = "htop"
source = "repo"

[[package]]
name = "rsync"
source = "repo"

[[package]]
name = "unzip"
source = "repo"

[[package]]
name = "which"
source = "repo"

[[package]]
name = "lsof"
source = "repo"
