# =============================================================================
# SysForge package manifest — starter stub
#
# Dual role (see DESIGN.md §Package Manifest):
#   - Pipeline `run packages` (bootstrap): every entry is installed.
#   - `sysforge update` / `sysforge build` (steady-state): entries are
#     build-rule overrides applied to the live install set.
#
# Fields per entry (all optional except name):
#   name           required  package name
#   source         optional  "repo" (pacman -S) | "aur" (AUR build) — set when
#                            classification is ambiguous or you want to force routing
#   pkgbuild_patch optional  true = extract and patch PKGBUILD flags before build
#   cache          optional  false = disable ccache/sccache (required for PGO)
#
# An entry with only `name` (or only `name` + `source`) has no override
# effect at steady-state — `source` is metadata only. `sysforge packages add`
# rejects such calls, and `sysforge update` warns once and treats the entry
# as inert.
#
# AUR packages are auto-cloned to [build] pkgbuild_src_dir on demand.
# =============================================================================

[build]
# Directory containing pre-cloned PKGBUILDs for aur/git packages.
# Each entry should have its own subdirectory: <pkgbuild_src_dir>/<name>/PKGBUILD
pkgbuild_src_dir = "~/src"
repo_mode = "profiled"  # "pacman" (default) | "profiled" — how repo-source packages are built
#                       # "pacman"   → repo packages only enter `sysforge update` scope
#                       #              if they carry a behavior-changing override.
#                       # "profiled" → every installed repo package is tracked. Override-
#                       #              tagged ones are source-built (pkgctl_checkout +
#                       #              makepkg); the rest take a fast path via batched
#                       #              `checkupdates` and one terminal `sudo pacman -Syu`
#                       #              after the source builds. Requires pacman-contrib.
#                       # per-package pkgbuild_patch = true overrides to source-build regardless
# (Deprecated: `update_repo_profiled = true` is normalised to repo_mode = "profiled".)


# =============================================================================
# Toolchain
# =============================================================================

[[package]]
name = "llvm"
source = "repo"
cache = false   # PGO build — never cache instrumented objects
[[package]]
name = "mesa-git"
source = "aur"
pkgbuild_patch = true
[[package]]
name = "cosmic-comp-git"
source = "aur"
pkgbuild_patch = true

[[package]]
name = "cosmic-panel-git"
source = "aur"
pkgbuild_patch = true

[[package]]
name = "cosmic-edit-git"
source = "aur"
pkgbuild_patch = true


# =============================================================================
# Audio
# =============================================================================
[[package]]
name = "egl-wayland-git"
source = "aur"
pkgbuild_patch = true
[[package]]
name = "flite1"
source = "aur"
pkgbuild_patch = true
[[package]]
name = "gtk2"
source = "aur"
pkgbuild_patch = true
[[package]]
name = "lcevcdec"
source = "aur"
pkgbuild_patch = true

[[package]]
name = "lib32-egl-wayland-git"
source = "aur"
pkgbuild_patch = true
[[package]]
name = "lib32-gperftools"
source = "aur"
pkgbuild_patch = true

[[package]]
name = "lib32-gtk2"
source = "aur"
pkgbuild_patch = true

[[package]]
name = "lib32-libappindicator-gtk2"
source = "aur"
pkgbuild_patch = true

[[package]]
name = "lib32-libdbusmenu-gtk2"
source = "aur"
pkgbuild_patch = true

[[package]]
name = "lib32-libgcrypt15"
source = "aur"
pkgbuild_patch = true

[[package]]
name = "lib32-libidn11"
source = "aur"
pkgbuild_patch = true

[[package]]
name = "lib32-libindicator-gtk2"
source = "aur"
pkgbuild_patch = true

[[package]]
name = "lib32-libjpeg6-turbo"
source = "aur"
pkgbuild_patch = true

[[package]]
name = "lib32-libpng12"
source = "aur"
pkgbuild_patch = true
[[package]]
name = "lib32-libtiff4"
source = "aur"
pkgbuild_patch = true
[[package]]
name = "lib32-libvpx1.3"
source = "aur"
pkgbuild_patch = true

[[package]]
name = "lib32-mesa-git"
source = "aur"
pkgbuild_patch = true

[[package]]
name = "lib32-openssl-1.0"
source = "aur"
pkgbuild_patch = true

[[package]]
name = "lib32-openssl-1.1"
source = "aur"
pkgbuild_patch = true

[[package]]
name = "lib32-sbc"
source = "aur"
pkgbuild_patch = true

[[package]]
name = "lib32-serd"
source = "aur"
pkgbuild_patch = true

[[package]]
name = "lib32-sord"
source = "aur"
pkgbuild_patch = true

[[package]]
name = "lib32-vulkan-icd-loader-git"
source = "aur"
pkgbuild_patch = true

[[package]]
name = "lib32-wayland-git"
source = "aur"
pkgbuild_patch = true

[[package]]
name = "lib32-zix"
source = "aur"
pkgbuild_patch = true
[[package]]
name = "libinput-git"
source = "aur"
pkgbuild_patch = true
[[package]]
name = "linux-steam-integration"
source = "aur"
pkgbuild_patch = true
[[package]]
name = "openapv"
source = "aur"
pkgbuild_patch = true
[[package]]
name = "pacman-static"
source = "aur"
pkgbuild_patch = true
[[package]]
name = "pop-launcher-git"
source = "aur"
pkgbuild_patch = true
[[package]]
name = "quirc"
source = "aur"
pkgbuild_patch = true
[[package]]
name = "rockchip-mpp"
source = "aur"
pkgbuild_patch = true
[[package]]
name = "volk-git"
source = "aur"
pkgbuild_patch = true
[[package]]
name = "wine-cachyos"
source = "aur"
pkgbuild_patch = true
[[package]]
name = "xavs2"
source = "aur"
pkgbuild_patch = true
[[package]]
name = "xeve"
source = "aur"
pkgbuild_patch = true
[[package]]
name = "xorg-xwayland-git"
source = "aur"
pkgbuild_patch = true
[[package]]
name = "lib32-gst-plugins-base"
source = "aur"
pkgbuild_patch = true
